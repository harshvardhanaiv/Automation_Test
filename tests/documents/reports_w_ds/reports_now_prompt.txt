Write a Playwright test script for AIV Reports. Follow these steps:
1. Navigate directly to the Reports section: 'https://aiv.test.oneaiv.com:8086/aiv/Documents/Reports'. Capture a screenshot named '02-reports-page.png'.
2. In the reports listing grid, find the cell containing 'Order details AutoTest' and double-click it to open the scheduler dialog. Capture a screenshot named '03-scheduler-open.png'.
3. Scope to the scheduler dialog container. Click on the 'Parameter' tab if visible. Find all editable text input fields inside the dialog (excluding readonly, disabled, hidden, radio, or checkbox inputs). For each of these inputs, clear its existing default value (such as '10100') and fill it with '10101'. Capture a screenshot named '04-parameters-filled.png'.
4. Go to the 'Schedule' tab inside the dialog and select the 'Right Now' radio option by clicking its text label directly. Capture a screenshot named '05-schedule-rightnow.png'.
5. Go to the 'Output' tab inside the dialog. Locate the input field for the output name (input[name="soutputname"]) inside the dialog, clear it, and type in a unique dynamic name using 'Run_' + Date.now(). Click the format dropdown (showing 'rptdocument') inside the dialog and select the 'pdf' option from the overlay list. Capture a screenshot named '06-output-setup.png'.
6. Click the 'Run' button inside the dialog. Capture a screenshot named '07-run-clicked.png'.
7. Navigate to the Requests section: 'https://aiv.test.oneaiv.com:8086/aiv/Request/Request'. Capture a screenshot named '08-requests-page.png'.
8. Poll (using up to 12 attempts with page reloads and 3-second delays) until the unique run name is visible in the requests table. Verify the run was listed and capture a screenshot named '09-run-verified.png'.
