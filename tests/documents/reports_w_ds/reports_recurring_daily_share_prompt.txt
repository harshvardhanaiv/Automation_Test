Write a Playwright test script for AIV Reports. Follow these steps:
1. Navigate directly to the Reports section: 'https://aiv.test.oneaiv.com:8086/aiv/Documents/Reports'. Capture a screenshot named '02-reports-page.png'.
2. In the reports listing grid, find the cell containing 'Order details AutoTest' and double-click it to open the scheduler dialog. Capture a screenshot named '03-scheduler-open.png'.
3. Scope to the scheduler dialog container. Click on the 'Schedule' tab. Under Frequency options, click the label containing 'Recurring' directly to select the 'Recurring' radio option. Capture a screenshot named '04-schedule-recurring-selected.png'.
4. Set Start Time as exactly 1 minute in the future and End Time as exactly 3 minutes in the future, formatted as 'DD/MM/YYYY HH:mm:ss' (e.g. if the current time is 10:20:00, set Start Time to 10:21:00 and End Time to 10:23:00). Capture a screenshot named '05-start-end-times.png'.
5. Set Pattern to 'Daily' and select the 'Every' radio button. Wait 3 seconds for the pattern settings to settle.
6. Locate the 'Share' checkbox label or input inside the dialog, and check/click it. Locate the 'Users' radio option label under Share inside the dialog, and click it. Locate the 'Visibility' dropdown under Share inside the dialog, click it, and select 'Public' from the options overlay list. Inside the 'Available Users' search input box in the dialog, type 'Demo' sequentially (letter-by-letter with a 100ms keypress delay) to trigger Angular's filter. Locate the visible cell containing exactly 'Demo', find its parent row, and click the checkbox wrapper/frame (.e-frame, .e-checkbox-wrapper, or .p-checkbox-box) inside it. Wait 5 seconds for AIV to register the selection. Capture a screenshot named '07-share-setup.png'.
7. Configure the daily pattern 'Time *' picker (relative to the required asterisk label container) at the very end of the Schedule tab setup to avoid it getting cleared. Set the value to exactly 2 minutes in the future (1 minute after Start Time) formatted in 12-hour AM/PM format (e.g. '10:22 AM') by removing the 'readonly' attribute and simulating keyboard input. If the setting does not register automatically, implement a 30-second manual fallback pause to allow the user to select the time manually in headed mode. Capture a screenshot named '06-pattern-configured.png'.
8. Switch to the 'Output' tab inside the dialog. Locate the input field for the output name (input[name="soutputname"]) inside the dialog, clear it, and type 'Recurring AutoTest'. Ensure format is 'rptdocument'. Capture a screenshot named '08-output-setup.png'.
9. Click the 'Run' button inside the dialog. Wait for the scheduler dialog to close, and capture a screenshot named '09-run-clicked.png'.
10. Navigate directly to the Requests section: 'https://aiv.test.oneaiv.com:8086/aiv/Request/Request'. Click the 'Schedule' tab. Adjust the date range filter by searching all inputs on the Requests page for the input containing the date separator ' - ' (which represents the active date filter), setting its value programmatically to '2026-07-05 - 2026-07-08', and clicking the 'Filter' button. Poll the grid (refreshing via button every 3 seconds up to 5 times) until the scheduled report 'Recurring AutoTest' appears. Capture a screenshot named '10-scheduled-list.png'.
11. Wait for 2.5 minutes (150 seconds) in the test code to let the schedule trigger and run.
12. Click on the top-right profile button and click the 'Logout' menu option. Clear all cookies, localStorage, and sessionStorage to isolate the session.
13. On the login screen, fill the email/username input field with 'Demo', fill the password input field with 'password', and click the 'Login' button.
14. Navigate to the Reports section via the sidebar menu UI (clicking the hamburger menu, selecting 'Documents', then clicking 'Reports') to ensure proper routing. Capture a screenshot named '11-reports-loaded.png'.
15. Search for 'Recurring AutoTest' in the reports grid. Poll (with grid refreshes and 3-second delays up to 10 attempts) until the file 'Recurring AutoTest' appears. Capture a screenshot named '12-file-appeared.png'.
16. Double-click the file 'Recurring AutoTest' to open it in a new tab.
17. Wait for the new browser tab that opens for the report. Wait for 60 seconds (1 minute) for the report to fully load and render in the new tab. Take a screenshot in the new tab named '13-demo-report-loaded.png' to verify successful loading, then close the tab.
