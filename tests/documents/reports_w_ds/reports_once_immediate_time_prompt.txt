Write a Playwright test script for AIV Reports. Follow these steps:
1. Navigate directly to the Reports section: 'https://aiv.test.oneaiv.com:8086/aiv/Documents/Reports'. Capture a screenshot named '02-reports-page.png'.
2. Click the hamburger menu button (using the selector 'button.smenu_button') at the top-left of the page to open/expand the sidebar menu. Capture a screenshot named '02b-sidebar-open.png'. Click the hamburger menu button again to close the sidebar.
3. In the reports listing grid, find the cell containing 'Order details AutoTest' and double-click it to open the scheduler dialog. Capture a screenshot named '03-scheduler-open.png'.
4. Scope to the scheduler dialog container. Click on the 'Parameter' tab if visible. Find all editable text input fields inside the dialog (excluding readonly, disabled, hidden, radio, or checkbox inputs). For each of these inputs, clear its existing default value (such as '10100') and fill it with '10101'. Capture a screenshot named '04-parameters-filled.png'.
5. Go to the 'Schedule' tab inside the dialog. Under Frequency options, select the 'Once' option by clicking its text label directly (which should select the 'Once' radio button). Locate the 'Start Time *' input field, clear it, and fill it with a date-time string exactly 30 seconds in the future, formatted as 'DD/MM/YYYY HH:mm:ss' (e.g., if current time is 15:33:10, set it to 15:33:40). Capture a screenshot named '05-schedule-once.png'.
6. Go to the 'Output' tab inside the dialog. Locate the input field for the output name (input[name="soutputname"]) inside the dialog, clear it, and type in a unique dynamic name using 'Run_' + Date.now(). Ensure the format dropdown is set to 'rptdocument' (select it if it's not already). Capture a screenshot named '06-output-setup.png'.
7. Click the 'Run' button inside the dialog. Wait for the scheduler dialog to close, and capture a screenshot named '07-run-clicked.png'.
8. Wait for 30 to 40 seconds on the page to allow AIV to run the scheduled report in the background.
9. Locate the search box at the top of the page with the placeholder 'Search files and folders in current section', clear it, and type in the unique run name generated in step 6.
10. Click the circular refresh icon button (using the selector '.fa-arrow-rotate-right' or '.fa-rotate-90') to reload the grid so the newly generated report file appears. Wait for the search result row with the unique run name to become visible.
11. Double-click the cell containing the unique run name in the search results grid.
12. Intercept and wait for the new browser tab that opens for the report (up to 90 seconds timeout). Once the tab is open, wait for 60 seconds (1 minute) for the report to fully load and render completely. Take a screenshot of the executed report in the new tab named '07b-executed-report.png', and close the tab.
